<% provide(:title, "Edit label") %>

<h1>Edit label</h1>

<%= form_for(@label) do |f| %>
  <%= render 'shared/error_messages', object: f.object  %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control field-group' %>

  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>
